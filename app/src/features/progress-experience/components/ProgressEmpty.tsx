import { EmptyState } from "../../../components/EmptyState";

export function ProgressEmpty() {
  return <EmptyState icon="analytics-outline" title="No analytics yet" subtitle="Complete workouts, log nutrition, and check recovery to unlock the full athlete dashboard." />;
}
