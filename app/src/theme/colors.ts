/**
 * Static light color shortcut — prefer `useTheme().colors` in components.
 */
export { lightColors as colors } from "./lightColors";
export { darkColors } from "./darkColors";
export type { ColorPalette, ThemeMode } from "./colorTokens";
