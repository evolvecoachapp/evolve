"""AI engines and orchestration for the EVOLVE Coach.

Everything under this package is invoked exclusively by services (never
directly by ``api/`` routes), per ``EVOLVE_ARCHITECTURE.md`` §4 ("AI is
isolated") and ``backend.mdc``.
"""
